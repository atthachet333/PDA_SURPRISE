{
  "name": "pda-bliss-platform",
  "version": "1.0.0",
  "private": true,
  "description": "PDA BLISS COMPANY LIMITED — corporate platform with a private client workspace experience.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "start": "concurrently -n backend,frontend -c green,cyan \"npm --workspace backend run start\" \"npm --workspace frontend run preview\"",
    "typecheck": "npm --workspace backend run typecheck && npm --workspace frontend run typecheck && npm run typecheck:tools",
    "typecheck:tools": "tsc -p tools/media-pipeline/tsconfig.json",
    "test": "npm --workspace backend test && npm --workspace frontend test",
    "lint": "npm --workspace frontend run lint && npm run lint:tools",
    "lint:tools": "eslint --max-warnings 0 --ext .ts tools/media-pipeline",
    "clean": "rimraf frontend/dist backend/dist",
    "validate:prod": "npm run typecheck && npm run lint && npm run build",
    "start:prod": "npm --workspace backend run start",
    "media:init": "tsx --tsconfig tools/media-pipeline/tsconfig.json tools/media-pipeline/cli.ts init",
    "media:ingest": "tsx --tsconfig tools/media-pipeline/tsconfig.json tools/media-pipeline/cli.ts ingest",
    "media:check": "tsx --tsconfig tools/media-pipeline/tsconfig.json tools/media-pipeline/cli.ts check"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "concurrently": "^9.1.2",
    "rimraf": "^6.0.1",
    "sharp": "^0.34.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "esbuild@0.25.12": true,
    "sharp@0.34.5": false
  }
}
